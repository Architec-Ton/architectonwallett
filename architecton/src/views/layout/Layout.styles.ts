import { css } from '@linaria/core';

const Layout = css`
  border: 1px dashed #00f;
  min-width: 375px;
  max-width: 375px;
  min-height: 100vh;
  padding: 2rem;
`;

const loaded = css``;

export default { Layout, loaded };
