import { css } from '@linaria/core';

const Layout = css`
  border: 1px dashed #00f;
  min-width: 375px;
  max-width: 375px;
  height: 100%;
  padding: 2rem;
`;

const loaded = css``;

export default { Layout, loaded };
