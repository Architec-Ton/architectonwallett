import React from 'react';
import BottomNavBar from "../bottom-nav-bar/BottomNavBar.tsx";

const Swap = () => {
    return (
        <div>
            SWAP
            <BottomNavBar/>
        </div>
    );
};

export default Swap;